# Dependencias de tarea1, FIJADAS el 2026-09-21 desde un entorno virtual limpio (Python 3.12.13, macOS arm64) en el que pasó toda la suite de tests
# y se abrió la app. Se fijan solo los paquetes directos; los transitivos los resuelve pip.
# Python 3.12 recomendado (las ruedas de PyTorch/ChromaDB tardan en llegar a versiones muy nuevas).
#
# En Windows instala PyTorch para CPU ANTES que el resto (evita descargar la versión con CUDA, ~2 GB):
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
# (sentence-transformers necesita torch; con el comando de arriba ya queda instalado; en la prueba se usó torch 2.14.0)

# Configuración
PyYAML==6.0.3
python-dotenv==1.2.3

# Extracción (proceso OFFLINE): PDF -> texto por página, OCR
PyMuPDF==1.28.2
pillow==12.3.0
pytesseract==0.3.13          # requiere el ejecutable de Tesseract (ver README: TESSERACT_CMD)

# Indexación y recuperación
numpy==2.5.3
sentence-transformers==6.1.0   # embeddings locales (multilingual-e5-small)
chromadb==1.5.9                # almacén persistente de vectores, textos y metadatos (la búsqueda es exacta, ver README)
openai==3.16.2                 # solo para la comparación de embeddings por API (Fase 6; opcional)

# Generación
requests==2.34.2               # llamadas REST a la API de Gemini (LLM y embeddings)
anthropic==1.7.0               # cliente de Claude: proveedor alternativo conservado, sin usar (llm.provider: anthropic)
tzdata==2026.4                 # zonas horarias en Windows (precios por hora)

# Interfaz web (Fase 7)
streamlit==1.64.0
pandas==3.0.6                  # lo usa app.py (streamlit ya lo instala)

# Backend siempre encendido + bot de Telegram (Fases 10 y 11)
fastapi==0.141.1
uvicorn==0.53.0
httpx==0.28.1                  # lo usa fastapi.testclient.TestClient (solo pruebas y desarrollo local)

# Reportes y gráficos
matplotlib==3.11.2

# Opcionales (no hacen falta para usar el proyecto):
#   tiktoken   -> estimación previa del costo de embeddings de OpenAI en evaluation/compare_embeddings.py
#   easyocr    -> solo scripts/ocr_benchmark.py (comparación de motores de OCR; arrastra PyTorch)
