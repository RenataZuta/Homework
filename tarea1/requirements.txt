# Dependencias de tarea1. Se amplía fase por fase, solo con paquetes realmente
# instalados y probados; al cerrar la Fase 7 se fijan versiones desde un venv limpio.
# Python 3.12 recomendado (las ruedas de PyTorch/ChromaDB tardan en llegar a versiones muy nuevas).
pyyaml>=6.0
python-dotenv>=1.0
